import React from "react"; export const Knob = ({ label, value, min, max, onChange, color = "#00ffc8", fmt = (v) => v.toFixed(1) }) => { const handleMouseDown = (e) => { e.preventDefault(); const startY = e.clientY; const startVal = value; const onMove = (me) => { const delta = (startY - me.clientY) * ((max - min) / 150); onChange(Math.min(max, Math.max(min, startVal + delta))); }; const onUp = () => { window.removeEventListener("mousemove", onMove); window.removeEventListener("mouseup", onUp); }; window.addEventListener("mousemove", onMove); window.addEventListener("mouseup", onUp); }; return ( <div style={{ textAlign: "center", cursor: "ns-resize", padding: "5px" }} onMouseDown={handleMouseDown}> <div style={{ width: 30, height: 30, borderRadius: "50%", border: `2px solid ${color}`, margin: "0 auto" }} /> <div style={{ fontSize: "9px", color: color, marginTop: "2px", fontWeight: "bold" }}>{fmt(value)}</div> <div style={{ fontSize: "8px", color: "#6e7681", textTransform: "uppercase" }}>{label}</div> </div> ); };
