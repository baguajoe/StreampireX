# Manual DB migrations

Raw SQL migration files for changes that happen outside the Alembic
framework in `../migrations/`. Run these by hand in Railway's psql
shell BEFORE the corresponding code-change deploy goes live.

After running, leave the file here as a record. Don't delete.
